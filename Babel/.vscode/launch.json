{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug babel",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/babel",
            "args": ["${workspaceFolder}/build/test.bbl"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build"
        }
    ]
}